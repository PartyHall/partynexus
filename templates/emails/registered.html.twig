{% extends 'emails/base.html.twig' %}

{% block content %}
    <p>{{ 'emails.registered.text1' | trans }}</p>

    <center>
        <button class="expanded" href="{{ link }}">{{ 'emails.registered.link' | trans }}</button>
    </center>

    <p>{{ 'emails.registered.warning' | trans }}</p>
{% endblock %}
